use io
use conv

main(args:int[][]){
    i:int = 0
    x:int = 1
    y:int = 1
    while(i<130000000){
        x = x + 1
        y = y + 1
        _ = fun(x, y)
        i = i + 1
    }
}

fun(x:int, y:int):int {
    z:int = x + y
    a:int = z / (x + y)
    return a
}